# Runtime data (SQLite DB + encrypted uploads)
/data/
*.db
*.db-wal
*.db-shm

# Local env
.env

# Local planning docs — kept out of the public repo
PLAN.md

# Built binary
/boating-accident

# Frontend
web/node_modules/
# Built SPA assets are generated; only the placeholder index.html is tracked.
web/dist/assets/

# OS
.DS_Store
